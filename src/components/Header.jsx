const Header = () => {
    return (
        <header>
            <h1>Where in the world?</h1>
            <button aria-label="Activate dark mode">
                dark mode
            </button>
        </header>
    )
}

export default Header;